# 배포용 환경 변수 템플릿
# 실제 배포 시에는 이 값들을 GitHub Secrets 또는 Cloud Run 환경변수로 설정

# Google Cloud 프로젝트 설정
GOOGLE_CLOUD_PROJECT=your-project-id
GCP_REGION=asia-northeast1

# Firebase 설정
FIREBASE_PROJECT_ID=emotion-analysis-system

# API 서버 설정
API_HOST=0.0.0.0
API_PORT=8080
DEBUG=false

# 인증 파일 경로 (컨테이너 내부)
GOOGLE_APPLICATION_CREDENTIALS=/app/config/firebase_service_account.json
OAUTH_CREDENTIALS_PATH=/app/config/google_credentials.json

# 로깅 설정
LOG_LEVEL=INFO

# CORS 설정
ALLOWED_ORIGINS=*

# 시간대 설정
TZ=Asia/Seoul